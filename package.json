{
  "name": "amp-runner-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Amp plugin for managing temporary, project-scoped runners on macOS",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "bun run test && bun run typecheck && bun run format:check"
  },
  "devDependencies": {
    "@ampcode/plugin": "0.0.0-20260826001054-g2427cf3",
    "@types/bun": "latest",
    "prettier": "latest",
    "typescript": "latest"
  }
}
